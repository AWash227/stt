I would like to add a text bubble that pops up next to the blob as I am speaking, showing what I've been saying. The text bubble should also have above it a collapsed list of historical buffers of text. These bubbles should pull the history from the narration.jsonl file. I want to be able to click on the active bubble (which should be at the bottom and larger than the others) and it should open an editor so that I can edit the text in the buffer. If I click on a historical entry, it should save the current active buffer, switch to the historical buffer, and then anything I say should be appended to the new active buffer (which is the historical buffer I clicked on). Do not delete history, when I click on one of the historical buffers, it should COPY the historical buffer's text into the NEW active buffer. Finally, please make the design of the text bubbles have an apple-like feel. I want them to have rounded corners, and a transparent / translucent blurred background. Ensure the text is legible. Make it easy to copy and paste from a buffer.
